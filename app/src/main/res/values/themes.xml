<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyNetWork" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
    </style>

    <style name="Theme.Fab" parent="ShapeAppearanceOverlay.Material3.FloatingActionButton">
        <item name="android:backgroundTint">@color/blue</item>
        <item name="textAllCaps">false</item>
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="Theme.MyNetWork.MaterialButton" parent="Widget.MaterialComponents.Button" >
        <item name="iconTint">?attr/colorControlNormal</item>
        <item name="android:checkable">true</item>
        <item name="android:textColor">?attr/colorControlNormal</item>
        <item name="android:textAllCaps">false</item>
        <item name="cornerRadius">@dimen/common_spacing</item>
        <item name="android:backgroundTint">@android:color/background_light</item>
        <item name="minWidth">@dimen/min_size_button</item>
        <item name="minHeight">@dimen/min_size_button</item>
    </style>

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/rounded_dialog</item>
    </style>
</resources>